#include <time.h>
#include <stdio.h>
int main(void){
    time_t first, second;
    // clrscr();

    // first = time(NULL);
    // delay(2000);

    // second(2000);
    // printf("The Interval is: %f seconds \n", difftime(second, first));
    // getch();
    // return 0;
}