#include <stdio.h>
#include <string.h>

int main(void)
{
    char str[11] = "0123456789";
    char symbol = 'a';
    printf("Before: %s\n", str);
    // strset(str, symbol);
    printf("After: %s\n"), str;

    getchar();
    return 0;
}