#include <stdio.h>
int main(void)
{
    // FILE *file;
    // file = fopen("test.txt", "w+");
    char string[30];
    printf("input a string:");
    // gets(string);
    printf("The string input was: %s\n", string);

    return 0;
}